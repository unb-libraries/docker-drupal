location /admin {
    include /etc/nginx/fastcgi_params;
    fastcgi_param PHP_VALUE "newrelic.enabled=false";
}

location = /health {
    include /etc/nginx/fastcgi_params;
    fastcgi_param PHP_VALUE "newrelic.enabled=false";
}
